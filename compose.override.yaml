services:
  bookingapp.ui:
    build:
      target: dev
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./BookingApp.UI:/app
      - /app/node_modules